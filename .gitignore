# Python
__pycache__/
*.py[cod]
*.pyo

# Virtual Environment
venv/
.env

# Django
db.sqlite3
media/

# Static files (if collected)
staticfiles/

# VS Code
.vscode/

# OS files
.DS_Store
Thumbs.db

# Python cache
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Environment variables
.env

# Logs
*.log

# Security pipeline: local scanner output (see docs/security.md §12).
# CI writes these into an ephemeral runner workspace and uploads them as
# workflow artifacts; a local run of the same commands would otherwise
# leave these behind in the repo root.
bandit-results.sarif
semgrep-results.sarif
pip-audit-results.json
gitleaks-results.sarif
trivy-*-results.sarif
zap-report.json
zap-report.html
zap.yaml