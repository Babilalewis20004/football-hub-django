# Technology Stack

Every entry below is confirmed either in `requirements.txt`/`requirements-dev.txt`, `INSTALLED_APPS`, `MIDDLEWARE`, or direct imports in the source. Nothing is listed speculatively.

## Backend

| Technology | Version (pinned) | Role |
|---|---|---|
| Python | 3.12 (per `SECURITY_AUDIT_2026-08-10.md` environment; `.pyc` files targets `cpython-312`) | Runtime |
| Django | 5.2.17 | Web framework. Note: `config/settings.py`'s docstring header says "Generated by 'django-admin startproject' using Django 6.0.6" — that is boilerplate left over from project creation and does **not** reflect the pinned/installed version; `requirements.txt` and the security audit both confirm 5.2.17 is what actually runs. |
| python-decouple | 3.8 | Reads configuration from `.env` / environment variables (`config/settings.py`) |
| nh3 | 0.3.6 | Server-side HTML sanitizer for post content (`blog/forms.py`) |
| qrcode | 8.2 | Generates the TOTP enrollment QR code image (`users/views.py`) |

## Database

| Technology | Version (pinned) | Role |
|---|---|---|
| PostgreSQL | Not determinable from the codebase (no version pin in code/docs) | Sole database engine (`django.db.backends.postgresql`) |
| psycopg2-binary | 2.9.10 | PostgreSQL DB-API driver |

## Real-time

| Technology | Version (pinned) | Role |
|---|---|---|
| Django Channels | 4.3.2 | ASGI WebSocket support for the `chat` app |
| Daphne | 4.2.3 | ASGI server; also registered in `INSTALLED_APPS` (required for Channels' `runserver` integration in dev) |
| channels-redis | 4.3.0 | Redis-backed channel layer, used **only when `REDIS_URL` is set** |
| Redis | Not determinable from the codebase (no version pin) — **optional**, used only if `REDIS_URL` is configured | Channel layer backend in production/multi-process deployments |

## Frontend

| Technology | Version | Role |
|---|---|---|
| Django Templates | Django 5.2's built-in template engine | Server-side rendering (`templates/`) |
| Bootstrap | 5.3.3, loaded from `cdn.jsdelivr.net` | CSS/JS UI framework |
| Bootstrap Icons | Loaded from `cdn.jsdelivr.net` | Icon set |
| HTMX | 1.9.12, loaded from `unpkg.com` | Partial-page updates: like/bookmark buttons, live-refreshing author/editor dashboards |
| Project CSS | — | Hand-written CSS organized into `static/css/core/`, `static/css/components/`, `static/css/pages/` |
| Project JS | — | Hand-written vanilla JS in `static/js/` (no frontend build step, bundler, or framework) |

## Authentication / Security

| Technology | Version (pinned) | Role |
|---|---|---|
| Django auth (`django.contrib.auth`) | Bundled with Django | Base authentication/authorization, `Group`/`Permission` model |
| django-otp | 1.7.0 | TOTP 2FA (`otp_totp` plugin) and recovery codes (`otp_static` plugin) |
| django-simple-captcha | 0.7.0 | CAPTCHA challenges on login/registration |
| django-csp | 4.0 | Content-Security-Policy header management (Report-Only mode — see [security-architecture.md](security-architecture.md)) |
| Django's password hashing | Bundled (PBKDF2 default) | Password storage |

## Content

| Technology | Version (pinned) | Role |
|---|---|---|
| django-ckeditor | 6.7.2 | Rich-text editor widget for `Post.content` (bundles CKEditor 4.22.1 — flagged EOL by `manage.py check`, see [security-architecture.md](security-architecture.md)) |
| django-taggit | 6.1.0 | Free-form post tagging |
| django-crispy-forms | 2.3 | Form rendering helper (`{{ form\|crispy }}`, used by password-reset templates) |
| crispy-bootstrap5 | 2025.4 | Bootstrap 5 template pack for crispy-forms |

## Integrations

| Technology | Version (pinned) | Role |
|---|---|---|
| python-telegram-bot | 22.8 | Sends a one-way "new post published" announcement to a Telegram channel (`blog/services/telegram.py`); optional, disabled unless `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHANNEL_ID` are set |

No other external API integrations (no email service beyond Django's console/SMTP backend abstraction, no SMS, no payment gateway, no third-party auth/SSO) exist in the codebase.

## Testing

| Technology | Version (pinned) | Role |
|---|---|---|
| pytest | 9.1.1 | Test runner (`requirements-dev.txt`) |
| pytest-django | 4.14.0 | Django integration for pytest (`pytest.ini`) |
| pytest-cov | 7.1.0 | Coverage reporting |
| Django's own `TestCase` / `manage.py test` | Bundled | Also usable directly; the project's tests are written as standard Django `TestCase` classes and run through either runner |

## Deployment-related (present in dependencies; see [deployment-architecture.md](deployment-architecture.md) for how they're actually wired)

| Technology | Version (pinned) | Role |
|---|---|---|
| Gunicorn | 23.0.0 | WSGI server dependency — present in `requirements.txt`, retained (not removed) but **not invoked** in the Docker setup; the ASGI production command is `daphne`, run directly by `docker/entrypoint.sh`. See `docs/docker.md` and `deployment-architecture.md`. |
| Daphne | 4.2.3 | ASGI server — required to serve WebSocket traffic; also listed under Real-time above. Production start command: `daphne -b 0.0.0.0 -p 8000 config.asgi:application` |
| WhiteNoise | 6.9.0 | Serves static files directly from the Django/ASGI process (`whitenoise.middleware.WhiteNoiseMiddleware`) |
| Docker | **Used**, as of this Dockerization — `Dockerfile`, `docker-compose.yml`, `docker-compose.override.yml` (dev), `docker-compose.prod.yml`, `docker/entrypoint.sh`, `docker/nginx/default.conf`, `.dockerignore`. See `docs/docker.md`. | n/a |
| CI/CD | **Not used** — no `.github/workflows/`, `.gitlab-ci.yml`, or other CI config found anywhere in the repository | n/a |

## Explicitly confirmed absent (do not assume these exist)

- No Django REST Framework (removed as an unused dependency per `SECURITY_AUDIT_2026-08-10.md`).
- No JavaScript framework (React/Vue/etc.) and no frontend build tooling (webpack/vite/npm `package.json`).
- No CI/CD pipeline configuration.
- No SQLite usage (gitignored defensively, never referenced in settings).
- No caching framework configured (`CACHES` setting is not overridden from Django's default local-memory backend, and nothing in the codebase reads/writes through Django's cache API).
- No object storage (S3 or similar) for media — Docker production media persistence is a single-host volume, see `docs/docker.md`.
